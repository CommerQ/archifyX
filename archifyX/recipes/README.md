# Recipes

| File | Role |
|---|---|
| `platform.mjs` | Platform Atlas / module-pack recipes (archifyX-specific) |
| `../engine/recipes/scenarios.mjs` | Leaf diagram scenario recipes (bundled engine) |

CLI: `node bin/archifyX.mjs guide "<scenario>" --json` uses the engine guide today.
Platform recipes are for agents reading this folder + cookbook.
